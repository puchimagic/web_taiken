# pptx資料の編集について

`Webプログラミング体験_資料.pptx`（体験授業本番用スライド）は `scripts/build_pptx.py` から生成している。

- スライドを直したいときは、`Webプログラミング体験_資料.pptx` を直接いじらず、`scripts/build_pptx.py` の該当箇所だけを `Edit` で差分修正してから `python3 scripts/build_pptx.py` を再実行する。
- スライド全体をゼロから作り直すと生成コストが大きいので避ける。既存スクリプトの再利用・差分編集を優先する。
- `Webプログラミング体験_資料.pptx`自体はgit管理下でコミット対象（.gitignoreでは無視されていない）。無視されているのは過去の講師個別資料（`2024*.pptx`・`2026*.pptx`）のみ。

# bat関連の作業について

⚠️ `Webプロセットアップ.bat` / `Webプロサーバー起動.bat` / `Webプロクリーン.bat`（体験授業当日用のWindowsセットアップ・起動・後片付けスクリプト）は他のファイルと異なり、UTF-8ではなく Shift_JIS（CP932）で保存されている。日本語Windows既定のcmd.exeがUTF-8をCP932として誤読し、`rem`コメント行が誤ってコマンドとして実行されてしまう不具合が実機で確認されたため。`Edit`ツールは通常UTF-8で読み書きするので、これらのファイルを編集した後は必ずCP932へ再エンコードすること。再エンコードを忘れてUTF-8のまま保存すると、cmdウィンドウが正常に動作しない不具合が再発する。

再エンコード例（Python）:
```python
raw = open('Webプロセットアップ.bat', 'rb').read()
open('Webプロセットアップ.bat', 'wb').write(raw.decode('utf-8').encode('cp932'))
```
（`Edit`ツールで一旦UTF-8として編集 → 上記スクリプトでCP932に戻す、という手順を取ればよい。）
