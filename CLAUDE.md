# pptx資料の編集について

`Webプログラミング体験_資料.pptx`（体験授業本番用スライド）は `scripts/build_pptx.py` から生成している。

- スライドを直したいときは、`Webプログラミング体験_資料.pptx` を直接いじらず、`scripts/build_pptx.py` の該当箇所だけを `Edit` で差分修正してから `python3 scripts/build_pptx.py` を再実行する。
- スライド全体をゼロから作り直すと生成コストが大きいので避ける。既存スクリプトの再利用・差分編集を優先する。
- 生成されたpptxは `.gitignore` で無視されている（`*.pptx`）。

# bat関連の作業について

`Webプロセットアップ.bat` / `Webプロサーバー起動.bat` / `Webプロクリーン.bat`（体験授業当日用のWindowsセットアップ・起動・後片付けスクリプト）を編集・調査するときは、先に `batファイルの説明.md` を読むこと。各batが何をするか、過去に起きた問題の原因と対策が記載されている。

⚠️ **この3つの `.bat` ファイルは他のファイルと異なり、UTF-8ではなく Shift_JIS（CP932）で保存されている。** 日本語Windows既定のcmd.exeがUTF-8をCP932として誤読し、`rem`コメント行が誤ってコマンドとして実行されてしまう不具合が実機で確認されたため。`Edit`ツールは通常UTF-8で読み書きするので、これらのファイルを編集した後は必ずCP932へ再エンコードすること（手順は `batファイルの説明.md` 参照）。再エンコードを忘れてUTF-8のまま保存すると、cmdウィンドウが正常に動作しない不具合が再発する。
