# bat ファイルの説明

体験授業のPC準備・PHPサーバー起動に使うWindows用batファイルは2つある。

## Webプロセットアップ.bat

**体験授業当日、各PCで最初に1回（生徒が入れ替わるたびに何度でも）実行するファイル。** デスクトップに配置してダブルクリックする想定。

リポジトリのcloneから開発環境オープンまでを次の順序で自動で行う。実行するたびにプロジェクトを削除して新しくcloneし直すため、常にまっさらな状態から始められる（前の生徒の投稿データや変更が残らない）。

1. **リポジトリを取得**
   - `git` コマンドの有無を確認（無ければエラーで停止）
   - デスクトップに `web_taiken` フォルダが既にあれば、削除してから改めて `git clone` する（無ければそのままclone）
   - 削除の前に、ポート8000で起動中のPHPサーバーがあれば停止し、VSCode（`Code.exe`）が起動していれば終了する（ファイルが使用中だと削除に失敗するため）
   - 削除に失敗した場合はエラーで停止する
   - clone成功後、`.git` フォルダと `README.md` を削除する（体験授業では不要で、VSCode上に見えるとかえって紛らわしいため。`.git`を消してもこのPC上の履歴が消えるだけで、GitHub側やこのリポジトリ自体には影響しない）
2. **VSCode拡張機能「PHP Server」を確認・インストール**
   - `code` コマンドの有無を確認（無ければエラーで停止）
   - 拡張機能 `brapifra.phpserver` が、後述の専用extensions-dir（`%VSCODE_EXTENSIONS%`）に入っていなければ `code --extensions-dir "%VSCODE_EXTENSIONS%" --install-extension` でインストール
3. **スライド(pptx)とサーバー起動用bat(Webプロサーバー起動.bat)をデスクトップに配置**
   - clone先にある `Webプログラミング体験_資料.pptx` と `Webプロサーバー起動.bat` をそれぞれデスクトップ直下にコピー
   - コピー後、プロジェクトフォルダ内の元ファイルは削除する（デスクトップとプロジェクトフォルダの二重管理を避けるため）
   - 見つからない場合は警告を出してスキップ（エラー停止はしない）
4. **VSCodeでプロジェクト専用プロファイルを使ってフォルダを開く**
   - この端末で普段使っているVSCodeの標準プロファイル（`%APPDATA%\Code`）は使わず、専用の `--user-data-dir`（`%DESKTOP_DIR%\.web_taiken_vscode_data`）を使う。これは普段のVSCodeのレイアウト記憶（サイドバー開閉状態など）や個人的にインストールしている拡張機能から完全に分離された領域
   - 開く前に `VSCODE_USER_DATA` を毎回削除してから作り直し、`"workbench.startupEditor": "none"` だけを書いた `User\settings.json` を配置する。これにより毎回VSCode本来の初期状態（プライマリサイドバーでエクスプローラーにフォーカス、セカンダリサイドバーは閉じた状態、余計な拡張機能のパネルも出ない）で開く
   - 拡張機能用の `--extensions-dir`（`%VSCODE_EXTENSIONS%`）は削除せず使い回すので、「PHP Server」拡張機能の再インストールは初回のみで済む
   - どちらのフォルダも `attrib +h` でデスクトップ上では非表示にする
   - `code --disable-workspace-trust --user-data-dir "%VSCODE_USER_DATA%" --extensions-dir "%VSCODE_EXTENSIONS%" "%TARGET_DIR%"` でclone先フォルダをVSCodeで開く（ワークスペース信頼ダイアログも省略）
5. **PHPサーバーを起動する**
   - `php` コマンドが無ければ、`:find_php` 処理でよくあるインストール先
     （`C:\php`、`C:\xampp\php`、`%LOCALAPPDATA%\Programs\php`）を探し、
     見つかればそのbatの実行中PATHと「ユーザー環境変数PATH」に追加する
     （システム環境変数PATHは触らない）
   - `php -S 127.0.0.1:8000 -t public` で組み込みサーバーを起動
   - `http://127.0.0.1:8000/login.php` にブラウザでアクセスする想定

途中でエラーになった場合は `pause` でコマンドプロンプトの表示を止め、
何かキーを押すまでウィンドウが閉じないようにしている（はずの)実装。

デスクトップの実際の場所はOneDrive「デスクトップと同期」を考慮し、レジストリ（`HKCU\...\User Shell Folders` の `Desktop` 値）から取得する（取得できない場合は `%USERPROFILE%\Desktop` にフォールバック）。

⚠️ ステップ1でのプロジェクトフォルダ削除前のVSCode終了は `taskkill /IM Code.exe /F` による強制終了で、保存確認ダイアログは出ない。未保存の変更があっても破棄される点に注意（直後にプロジェクトフォルダ自体を削除するため実質的な影響はないが、PC上の他のVSCodeウィンドウ（このプロジェクトと無関係なもの）が開いていた場合もまとめて終了する点は留意）。

過去バージョンで作っていた `web_taiken_cleanup_log.txt`（後述のクリーンアップ用batの名残）が残っていた場合は、起動時に自動で削除する。

### （経緯）VSCodeのレイアウトリセット方式の変更

当初はステップ4のレイアウトリセットを、標準プロファイル（`%APPDATA%\Code\User\workspaceStorage\`）内の該当ワークスペースの保存状態だけを探して削除する方式で実装していた。実機で確認したところ、対象ディレクトリの構造自体（`workspace.json` にフォルダのfile:// URIが記録されている等）は想定通りだったが、実際に試した際にセカンダリサイドバー（補助サイドバー）が開いたままになる現象が解消しなかった。原因ははっきり切り分けられていないが、この端末に個人的にインストールしている拡張機能（Claude Codeなど）のパネルがセカンダリサイドバー側に表示される設定になっていた可能性が高く、ワークスペース単位の状態削除だけでは対処しきれないと判断した。

そのため、標準プロファイルの状態を部分的に削除するのではなく、`--user-data-dir` / `--extensions-dir` でプロジェクト専用の隔離されたVSCodeプロファイルを使う方式に変更した。個人の拡張機能やレイアウト記憶を一切引き継がない状態から毎回開くため、副作用の余地がなくなる。

## Webプロサーバー起動.bat

**接続が切れた等でPHPサーバーだけ再起動したいときに使うファイル。** Webプロセットアップ.batの「3/5」でデスクトップにコピーされ、プロジェクトフォルダ内の元ファイルは削除されるため、デスクトップ上のこのファイルを直接実行する。

Webプロセットアップ.batの「5/5: PHPサーバーを起動する」と同じ処理だけを単独で行う。

1. `php` コマンドの有無を確認（`:find_php` の中身はWebプロセットアップ.batと同じ）
2. `php -S 127.0.0.1:8000 -t public` で組み込みサーバーを起動

DOCROOTは `%~dp0`（bat自身のあるフォルダ）ではなく、Webプロセットアップ.batと同じOneDrive対応のレジストリ参照で実際のデスクトップを求め、`<デスクトップ>\web_taiken\public` を直接指定する。これは、このbatが（プロジェクトフォルダの中ではなく）デスクトップ直下に置かれる前提に変わったため。`<デスクトップ>\web_taiken\public` が存在しない場合は、先にWebプロセットアップ.batを実行するよう促すエラーを出して停止する。

## （廃止）クリーンアップ用bat

体験授業終了後の後片付け専用に `Webプロクリーン.bat` という別batを一時期用意していたが、「Webプロセットアップ.bat自体を毎回、既存プロジェクトの削除＋再clone＋レイアウトリセットまで行う」設計に統合したことで不要になったため削除した。次の生徒の準備は `Webプロセットアップ.bat` を再実行するだけでよい。

`Webプロセットアップ.bat` の「3/5」ステップで、スライド(pptx)と同様にこのファイルもclone先からデスクトップへ自動コピーされる。そのため通常はデスクトップに手動で配置する必要はない（Webプロセットアップ.batを一度実行すればデスクトップに揃う）。

## 過去の課題と原因（2026-08-15、解決済み）

`Webプロセットアップ.bat` をWindows PCでダブルクリック実行したところ、
cmdウィンドウが一瞬（0.1秒程度）で閉じてしまい、正常に動作しなかった。

実機（Windows 11, cmd.exe）で再現テストを行い、以下の2つの実害あるバグを
特定・修正した。

### 原因1: 文字コード（UTF-8 → CP932の誤読でrem行が壊れる）

ファイルがUTF-8（BOM無し）で保存されていたが、日本語Windowsのcmd.exeは
既定でShift_JIS（CP932）としてバッチファイルを読む。UTF-8のマルチバイト
列をCP932として誤読すると、`\r\n`（改行）を巻き込んで別の1文字として
誤解釈されることがあり、その結果 `rem` コメント行の一部が「コマンドとして
認識されていません」というエラーとともに実際に実行されてしまうことを
実機で確認した（ファイル冒頭のrem説明文で発生）。

**対策として試して効かなかったもの:** ファイル先頭に `chcp 65001` を
追加してUTF-8のまま解釈させる方法。cmd.exeはbatファイルをある程度まとめて
先読みするため、chcp実行前に読み込み済みの範囲には効果がなく、実機テストで
効果なしと確認済み。

**採用した対策:** `Webプロセットアップ.bat` / `Webプロサーバー起動.bat` 本体のファイル自体を
**Shift_JIS（CP932）** で保存し直した（改行コードはCRLFのまま）。
これにより日本語Windows既定のcmd.exeが誤読なく解釈できることを実機で確認済み。

⚠️ **重要:** そのため、この2つの `.bat` ファイルは他のテキストファイルと異なり
**UTF-8ではなくCP932（Shift_JIS）で保存されている。** 今後Claude Code等の
エディタツール（通常UTF-8で読み書きする）でこれらのファイルを編集する場合は、
編集後に必ずCP932へ再エンコードすること。単純にUTF-8として上書き保存すると
このバグが再発する。再エンコード例（Python）:
```python
raw = open('Webプロセットアップ.bat', 'rb').read()
open('Webプロセットアップ.bat', 'wb').write(raw.decode('utf-8').encode('cp932'))
```
（Editツールで一旦UTF-8として編集 → 上記スクリプトでCP932に戻す、という
手順を取ればよい。）

### 原因2: `call :pause_and_exit N` がスクリプト全体を終了させていなかった

`:pause_and_exit` サブルーチンは末尾で `exit /b %~1` としていたが、
`call :pause_and_exit N` の形で**呼び出し(call)経由**で実行される
`exit /b` は、呼び出し元への「復帰」にしかならず、**バッチスクリプト
全体は終了しない**（cmd.exeの既知の仕様）。

このため、例えば「gitが見つからない」エラーでログ表示・pauseまでは
行われるものの、その後スクリプトが止まらずに後続のgit clone処理などへ
処理が続いてしまうことを実機テストで確認した。

**採用した対策:** `call :pause_and_exit N` を廃止し、`set "EXIT_CODE=N"`
してから `goto :pause_and_exit` する方式に変更（`goto`は呼び出し階層を
増やさないため、ラベル内の `exit /b` が正しくスクリプト全体を終了させる）。
また `:pause_and_exit` 内の `endlocal` は変数スコープを巻き戻して
`EXIT_CODE` を消してしまうため、`endlocal & exit /b %EXIT_CODE%` と
1行にまとめ、`endlocal` 実行前に `%EXIT_CODE%` を展開させる定石を使った。

以上2点の修正により、実機テスト（`where git` 相当のコマンドを一時的に
存在しないコマンドに差し替えてエラー分岐を強制発火させるテスト）で、
文字化けなくエラーメッセージが表示され、pause後に正しい終了コードで
スクリプトが停止する（後続処理へのフォールスルーが起きない）ことを確認済み。

### 原因3: `\"...\"` によるメッセージ中の引用符表現が、スペースを含むと壊れる

エラーメッセージ内で `\"PHP Server\"` のように、対象の単語をバックスラッシュ
エスケープした引用符で囲んで強調する書き方をしていた。しかしcmd.exeの
バッチパーサーはバックスラッシュによる引用符エスケープをサポートしておらず、
`\"` は単に「引用符を1個閉じる／開く」動作をする。`\"git\"` や `\"php\"` の
ように囲む語にスペースが無ければ閉じ引用符と開き引用符が隙間なく連結されて
たまたま1つの引数として成立していたが、`\"PHP Server\"` のようにスペースを
含む語では、引用符が閉じた時点で「PHP」と「Server\"...」が別々の引数に
分割されてしまい、`call :log` の呼び出しが壊れてメッセージが正しく
表示されない（ログにもステップ2/5の内容が欠落する）ことを実機の本番同様の
通し実行で確認した。

**採用した対策:** すべての `\"語句\"` 表現を、エスケープが不要な日本語の
鉤括弧 `「語句」` に置き換えた。鉤括弧はcmd.exeにとって特別な意味を持たない
ただの文字なので、内容にスペースが入っていても安全。

### 原因4: `code "%TARGET_DIR%"` を `call` 無しで呼んでいた

VSCodeのCLI `code` はWindowsでは実体が `code.cmd`（バッチファイル）である。
バッチファイルから別のバッチ/cmdファイルを `call` を付けずに直接呼び出すと、
呼び出し先が終了した時点で**呼び出し元のスクリプトには処理が戻らず、
cmd.exeプロセスごと終了してしまう**というcmd.exeの既知の仕様がある。

`Webプロセットアップ.bat` の「4/5: VSCodeでプロジェクトを開きます」で `code "%TARGET_DIR%"`
を `call` 無しで実行していたため、VSCodeが開いた時点でスクリプト全体が
無言で終了し、「5/5: PHPサーバーを起動する」以降が一切実行されない
（`pause` も出ない）ことを実機の本番同様の通し実行で確認した。
これは今回確認できた中で「pauseすら出ずに終了する」症状に最も近い、
最有力の原因候補と言える。

**採用した対策:** `code` を呼び出している3箇所（拡張機能一覧取得・拡張機能
インストール・プロジェクトを開く）すべてに `call` を付けた。

### 原因5（軽微）: `call :log ""` が空行の代わりに `ECHO は <OFF> です。` を出力していた

`:log` サブルーチンは `echo %~1` で引数を表示していたが、`call :log ""` の
ように空文字列を渡すと `echo %~1` が実質 `echo ` （引数無しのecho）になり、
cmd.exeの仕様で「現在のechoの状態（ON/OFF）」を表示してしまう
（`ECHO は <OFF> です。`）。空行を出したいだけなのにノイズが混じり、
ログファイルもこのノイズで汚れていた。

**採用した対策:** `echo %~1` を `echo(%~1` に変更。`echo(` は引数が空でも
正しく空行を出力できる定石。

---

以上5点の修正により、実機で **`Webプロセットアップ.bat` を実際にデスクトップに配置し、
ダブルクリック相当の方法で最初から最後まで通しで実行**し、以下をすべて確認した:

- 文字化けなし
- 1/5〜4/5のログ・メッセージがすべて正しく表示・記録される
- VSCodeが正しく開く
- 5/5でPHPが `where php` では見つからない場合でも `:find_php` のフォール
  バック（`C:\xampp\php` 等の探索）が正しく機能する
- `php -S 127.0.0.1:8000 -t public` が実際に起動し、
  `http://127.0.0.1:8000/login.php` にHTTP 200で応答する

なお、以下は今回のテストでは再現しなかったが、万一まだ問題が起きる場合の
追加候補として残しておく:
- エクスプローラーの「ファイル名を指定して実行」やネットワーク越しの
  ファイルに対するWindows SmartScreen等のブロック（この場合は警告ダイアログ
  が出るはずで、無音の瞬間終了とは挙動が異なる）
- 拡張子が実際には `.bat` になっていない（例: `.bat.txt`。エクスプローラーの
  「拡張子を表示」設定を有効にして確認するとよい）

## 原因6（2026-08-15、解決済み）: OneDrive「デスクトップと同期」でDesktopパスがずれる

`Webプロセットアップ.bat` を実際にデスクトップに配置してダブルクリック実行したところ、
ログ上は最後まで成功していた（`git clone`、VSCode起動、PHPサーバー起動まで
すべて完了）にもかかわらず、ユーザーからは「デスクトップにプロジェクトが
配置されていない」ように見える、という報告があった。

実機調査の結果、原因はOneDriveの「デスクトップと同期」（Known Folder Move）
機能だった。この機能が有効な環境では、エクスプローラー上で実際に見えている
デスクトップは `%USERPROFILE%\Desktop`（例: `C:\Users\<user>\Desktop`）ではなく
`%USERPROFILE%\OneDrive\デスクトップ` のような別の場所にリダイレクトされる。
一方 `Webプロセットアップ.bat` は `TARGET_DIR` や `LOG_FILE` を `%USERPROFILE%\Desktop` に
決め打ちしていたため、旧来の（今はユーザーの目に見えない）`Desktop` フォルダに
プロジェクトやログファイルを作ってしまい、ユーザーが実際に見ているデスクトップ
には何も現れなかった。

**採用した対策:** レジストリ
`HKCU\Software\Microsoft\Windows\CurrentVersion\Explorer\User Shell Folders`
の `Desktop` 値から実際のデスクトップパスを取得し、`DESKTOP_DIR` として
`TARGET_DIR`・`LOG_FILE`・スライドのコピー先すべてに使うよう変更した
（値が取得できない場合は従来どおり `%USERPROFILE%\Desktop` にフォールバック）。
`:find_php` が `HKCU\Environment` の `Path` を読む際と同じ `reg query` +
`for /f "usebackq tokens=2,*"` のパターンを使っている。

併せて、VSCodeでプロジェクトを開く際に毎回「ワークスペースの信頼」ダイアログ
（制限モード）が出て生徒が戸惑う可能性があったため、`call code` に
`--disable-workspace-trust` を付けて自動的に信頼済みとして開くようにした。

## 原因7（2026-08-15、解決済み）: ダブルクリックのたびに「発行元を確認できませんでした」と出る

`Webプロセットアップ.bat` をデスクトップに配置してダブルクリックするたびに、Windowsの
「開いているファイル - セキュリティの警告」（発行元を確認できませんでした）
ダイアログが毎回表示される、という報告があった。

実機で調査したところ、デスクトップ上の `Webプロセットアップ.bat` にNTFSの代替データ
ストリーム `Zone.Identifier`（いわゆるMark of the Web）が付与されていた。
`ZoneId=3`（インターネットゾーン）で、`ReferrerUrl` が `vscode-file://...`、
`HostUrl` が clone元フォルダ内の `Webプロセットアップ.bat` を指していたことから、
**VSCodeのエクスプローラーからデスクトップへドラッグ＆ドロップ（またはそれに
準じるコピー操作）した際に、Windowsがブラウザダウンロードと同様に
「ネットワーク越しに取得したファイル」とみなしてこの印を付けたもの**と判明した。
ブラウザ経由でzip等をダウンロードして配置した場合も同様の印が付く。

この印が付いたままだと、ダイアログで「実行」を選んでも（環境やコピー方法に
よっては）印が消えずに残り、ファイルを配置し直すたびに再現する。

**採用した対策:**
- 該当PC上の `Webプロセットアップ.bat` に対して `Unblock-File` （PowerShell）で
  `Zone.Identifier` ストリームを削除し、即時に警告を止めた。
- 恒久的な回避策として、`README.md` の「体験授業当日の準備（Windows）」に、
  配置後に一度だけ「プロパティ→許可する」にチェックするか
  `Unblock-File` を実行する、という案内を追記した。
  （bat自身は実行前のOSレベルの警告なので、bat本体の処理では対処できない。）

なお本件は bat ファイルの中身自体の不具合ではなく、配置手順（デスクトップへの
コピー方法）に起因するものである。

## ログ出力について（対応済み）

`Webプロセットアップ.bat` / `Webプロサーバー起動.bat` の両方に、実行内容をログファイルへ
記録する仕組みを追加した。

- `Webプロセットアップ.bat` 実行時: デスクトップに `web_taiken_setup_log.txt` を作成
- `Webプロサーバー起動.bat` 実行時: bat自身のあるフォルダに `web_taiken_start_log.txt` を作成

各ステップの開始・終了メッセージやエラー内容は `:log` サブルーチン
（画面表示とログファイル追記を同時に行う）を通して記録される。
`git clone` や拡張機能インストールなど、外部コマンドの出力自体も
`>> "%LOG_FILE%" 2>&1` でログファイルに追記される。

PHPサーバー起動（`php -S ...`、Ctrl+Cで止める常駐処理）だけは
リダイレクトせず、通常どおり画面にリアルタイム表示する
（ログにはサーバーを起動した旨のメッセージのみ残す）。

また、エラーで停止する場合は必ず `:pause_and_exit` サブルーチンを通り、
「ログを保存しました」と表示したうえで `pause` するため、
仮に途中で異常終了してもウィンドウが即座に閉じることはなくなったはず。

上記「過去の課題と原因」の2点を修正済みのため、通常はこれで解決しているはず。
念のため次回ダブルクリック実行した際は `web_taiken_setup_log.txt`（デスクトップ）
の中身を確認すること。ファイル本体がCP932（Shift_JIS）で保存されているため、
ログファイルもCP932で出力される（メモ帳等の標準エディタで問題なく開ける）。
それでも一瞬で閉じる場合は、「過去の課題と原因」末尾に挙げたSmartScreenや
拡張子誤りなどを疑う。
