# この体験授業で伝えたいこと

Webプログラミング体験として、参加者に「旅行スポット共有サイト」（PHP + SQLite、VSCode）の未完成部分を少しずつ完成させてもらい、「コードを直すとWebアプリの動きが変わる」ことを手を動かして体感してもらう。表側（フロントエンド）だけでなく、裏側（バックエンド）や外部APIとの連携を、料理・キッチンの比喩を交えて理解してもらうことを重視する。

このサイトは「知る人ぞ知る場所」を写真とタグで共有するサイトで、予約機能などはなく、個人が自由に投稿できる。

体験できる項目は以下の6つ。

（※エディタはVisual Studio 2022ではなくVSCodeを使用する。C#のような「プロパティ画面」や「ツールボックス」は存在しないため、すべてコードをテキストエディタで直接編集する体験になる。）

---

## 0. 導入：プログラミングって何？

- プログラミングとは「料理を作る作業」に近いことを伝える。情報システムを作る流れを、料理の工程に例えて説明する（要件定義＝メニューを決める、設計＝レシピを考える・材料や手順を調べる、実装＝調理する、テスト＝味見・修正する、運用保守＝食事・片付け）。比喩は料理ひとつに絞り、家づくりの例えは使わない（例えが複数あるとかえって分かりにくくなるため）。
- 今日の実習は、この中の「実装」「テスト」にあたる部分（表でいう赤字の部分）を簡単に体験する位置づけであることを伝える。
- Webページには「表の顔」（フロントエンド：HTML/CSS/JavaScript）と「裏の顔」（バックエンド：今回はPHP）があることを説明する。
  - HTML：Webページの骨組み／CSS：装飾（デザイン担当）／JavaScript：装飾（動き担当）というフロントエンドの役割分担も簡単に触れる。
  - 裏の顔（バックエンド）は利用者に見えない部分で処理を行い、利用者に合わせたWebページをその場で作れる（Aさんの投稿一覧とBさんの投稿一覧は載る情報が異なる、など）。表の顔だけではこれができない、という流れで裏の顔の必要性につなげる。
  - 「ラーメン」に例えると…
    - フロントエンドだけ＝カップラーメン（すぐ提供できるが全員同じ味）
    - バックエンドまで含む＝ラーメン屋さん（提供に少し時間はかかるが、お客さんに合わせた味・トッピングができる）
  - 今日は「厨房エリア（バックエンド）」を覗いてみる、という導入をする。

## 1. コードと動作の因果関係（コメントアウト体験）

タイピングを最小限にし、「既存のコードを少し直す」「コメントを外す」操作を中心に、「このコードのおかげでこう動く」「直すと変わる」を体感させる。VSCodeにはプロパティ画面やツールボックスがないため、すべてコードをその場で直接書き換える。

- **ボタンの表示文字を変更**（[public/upload.php](public/upload.php)）
  HTML内の投稿ボタンの文言をテキストエディタで直接書き換え、画面表示がその場で変わることを確認する（“味見”して確認、という言い方で一度動作確認を挟む）。
- **コメント投稿機能を有効化**（[public/comments.php](public/comments.php)）
  投稿処理を囲んでいる `/*` と `*/`（コメントアウト）を外すだけで、投稿できなかったコメントが投稿できるようになることを体験する。「`/* */` で囲まれた部分は実行時に無視される」という仕組みも合わせて説明する（例：`/* ここは実行時に無視される */`）。

## 2. APIとは何か（現在地取得と住所検索）

投稿ページの「📍 現在地を取得」ボタンを押すと、ブラウザの位置情報機能（Geolocation API）で緯度・経度を取得し、[public/geocode.php](public/geocode.php)を経由して外部の住所検索API（OpenStreetMap Nominatim）に緯度・経度を送信、住所が自動入力される。

- 「URLを叩けばデータが返ってくる」という外部APIの仕組みを、実際に自分の現在地で体験する。
- ブラウザ側の機能（位置情報取得）とサーバー側の機能（外部APIへの問い合わせ）が連携して1つの機能を実現していることを理解する。
- 「関数を1行呼ぶだけ」ではなく、実際に何が起きているか（緯度経度→サーバー経由で外部API→住所テキスト）を体感させる。
- 補足：この住所取得の仕組みは「関数」と呼ばれるまとまりで実現されていることに触れる。「郵便番号（緯度経度）を渡すと住所データが返ってくる」というやり取りを、システムへの依頼と回答というイメージで説明する。「機能をひとまとめにして作る」のもプログラマーの大事な仕事であることを伝える。

## 3. データベースとは何か

- コメント投稿・スポット投稿機能を通じて、「投稿ID」「投稿者」「タイトル」「タグ」「位置情報」「投稿日時」といった情報がデータとして保存・管理されていることを説明する（[src/db.php](src/db.php)の`spots`・`spot_tags`・`comments`テーブル）。
  - 「コメントを特定するにはどんな情報が必要そうか」を参加者に問いかけながら、コメントID・投稿先のスポット・投稿日時・本文といった要素を一緒に洗い出す進め方もできる。
  - データを整理して管理・利用する技術を「データベース」ということ、AI時代においてもますます重要な技術であることを伝える。
- タグを使った絞り込み検索（[public/index.php](public/index.php)）を通じて、データベースへの問い合わせ（SQL）で欲しい情報だけを取り出せることを体感する。
- 発展：住所の自動入力・郵便番号検索も「データのかたまり」を検索している点は同じであることに触れ、扱いやすくデータを整理することも大事な仕事であると伝える。

## 4. 入力チェック（バリデーション）

- コメント欄が空白のままでも投稿できてしまう状態を確認し、「実際のシステムでは入力漏れのチェックが必須」であることを体験する。
- VSCodeにはツールボックスがないため、[public/comments.php](public/comments.php)に用意された空欄チェックのコード（`/* */`でコメントアウトされたif文）を一緒に読み、`/*` `*/` を外して実際に有効化することで、入力チェック機能を実装する体験にする。
- 「難しそうに見えるが、実際のシステムでもよく使われる基本的な機能である」ことを伝え、抵抗感を減らす。

## 5. アレンジ課題（任意・発展）

タグの表示や、コメント表示部分の文言（例：「さん」を「様」に変更するなど）を自分で工夫して変えてみる、簡単な発展課題を用意し、「言われた通り」ではなく自分で考えて直す体験もできるようにする。

## 6. 準備のしやすさ

`start-mac.sh` / `start-win.bat` を実行するだけで、VSCodeとPHPさえ入っていれば旅行共有サイトのWebアプリがその場で起動する。

## 7. まとめ・締めのメッセージ

- 今日の体験（ボタン文言の変更、コメント投稿の有効化、入力チェックの追加など）は、いずれも実際のWebプログラマーの仕事の入口であることを伝える。「小さな修正をして、動かして、確認する」という流れは、規模は違っても実務の開発サイクルと同じであることを強調する。
- Webプログラマーの仕事は「コードを書くこと」だけでなく、要望を聞く→設計する→実装する→テストする→公開する→改善する、という一連の流れの一部であることに触れる。
- 「表側」だけでなく「裏側」（処理・データ・認証・外部サービス連携）や、テスト・セキュリティ・性能・運用保守といった、利用者から見えない部分こそ重要であることを伝える。
- Webの仕事は一人で完結せず、利用者・SE/PM・デザイナー・インフラエンジニア・セキュリティ担当など、多くの人との連携で進むことにも触れられるとよい（発展）。
- 締めとして、「今日の体験授業で“最初の一歩”は達成」という前向きな言葉で終える。

---

## 体験の流れ（想定）

1. プログラミングとは何か、フロントエンドとバックエンドの違いを簡単に説明する（料理・ラーメンの例え）
2. `start-mac.sh` などでシステムを起動し、旅行共有サイトの画面を確認する
3. ログイン画面→新規登録画面へ進み、投稿ボタンの表示が分かりにくいことに気づく
4. [public/upload.php](public/upload.php) のボタン文言をテキストエディタで直接修正 → ブラウザで再読み込みして“味見”（確認）する
5. 会員登録を行い、ログイン状態にする（ブラウザを閉じる前にログアウトする旨を案内）
6. 投稿ページで「現在地を取得」ボタンを押し、位置情報から住所が自動入力される様子を確認する（APIの実演。裏で何が起きているか＝緯度経度→サーバー経由で外部API→住所テキスト、を説明する）
7. 画像とタグを入力してスポットを投稿してみる
8. ホーム画面でタグをクリックし、絞り込み検索を確認する
9. コメント投稿を試すが、投稿できないことを確認する
10. [public/comments.php](public/comments.php) の該当箇所から `/*` `*/` を外し、コメント投稿を有効化 → 再度投稿して確認
11. （任意）コメント表示やタグ表示の文言をアレンジしてみる
12. 投稿・タグ・位置情報の各データがどう保存されているか、「コメントに必要な情報は何か」を一緒に考えながらデータベースの考え方を説明する
13. 入力漏れチェックの機能がないことを確認し、[public/comments.php](public/comments.php)に用意された空欄チェックのif文から `/*` `*/` を外して有効化し、空欄投稿がチェックされることを確認する
14. 今日体験したことが実際のWebプログラマーの仕事の一部であることを振り返り、締めのメッセージで終える
