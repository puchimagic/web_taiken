# この体験授業で伝えたいこと

Webプログラミング体験として、参加者に「動画共有サイト」（PHP + SQLite、VSCode）の未完成部分を少しずつ完成させてもらい、「コードを直すとWebアプリの動きが変わる」ことを手を動かして体感してもらう。表側（フロントエンド）だけでなく、裏側（バックエンド）の存在と役割を、料理・キッチンの比喩を交えて理解してもらうことを重視する。

体験できる項目は以下の6つ。

（※エディタはVisual Studio 2022ではなくVSCodeを使用する。C#のような「プロパティ画面」や「ツールボックス」は存在しないため、すべてコードをテキストエディタで直接編集する体験になる。）

---

## 0. 導入：プログラミングって何？

- 情報システムを作る流れを「家を建てる」「料理を作る」に例えて説明する（要件定義＝お客さんの要望を聞く／メニューを決める、設計＝設計図・レシピ、実装＝建築・調理、テスト＝検査・味見、運用保守＝入居・食事）。
- 今日の実習は、この中の「実装」「テスト」にあたる部分を簡単に体験する位置づけであることを伝える。
- Webページには「表の顔」（フロントエンド：HTML/CSS/JavaScript）と「裏の顔」（バックエンド：今回はPHP）があることを説明する。
  - フロントエンドだけ＝カップラーメン（すぐ提供できるが全員同じ味）
  - バックエンドまで含む＝ラーメン屋さん（少し時間はかかるがお客さんに合わせた味・トッピングができる）
  - 今日は「厨房エリア（バックエンド）」を覗いてみる、という導入をする。

## 1. コードと動作の因果関係（コメントアウト体験）

タイピングを最小限にし、「既存のコードを少し直す」「コメントを外す」操作を中心に、「このコードのおかげでこう動く」「直すと変わる」を体感させる。VSCodeにはプロパティ画面やツールボックスがないため、すべてコードをその場で直接書き換える。

- **ボタンの表示文字を変更**（[public/upload.php](public/upload.php)）
  HTML内の投稿ボタンの文言をテキストエディタで直接書き換え、画面表示がその場で変わることを確認する。
- **住所検索の仕組みを知る（概念説明）**
  「郵便番号を渡すと住所データが返ってくる」という仕組みを、ローカルに用意した住所データから検索するイメージで説明する（※今回のPHP版では実装しておらず、考え方の紹介にとどめる）。
- **コメント投稿機能を有効化**（[public/comments.php](public/comments.php)）
  該当行の先頭についている `//`（コメントアウト）を外すだけで、投稿できなかったコメントが投稿できるようになることを体験する。「`//` の後ろは実行時に無視される」という仕組みも合わせて説明する。

## 2. 関数とは何か

「郵便番号を渡すと住所データが返ってくる」ような例を使い、「呼び出す・結果が返ってくる」という関数の仕組みを説明する。「機能をひとつのまとまりとして作っておき、必要なときに呼び出す」という考え方を理解してもらう。

## 3. データベースとは何か

- コメント投稿機能を通じて、「コメントID」「投稿された動画」「投稿日時」「コメント本文」といった情報がデータとして保存・管理されていることを説明する（[src/db.php](src/db.php)の`comments`テーブル）。
- データを整理して扱いやすくすることもプログラマーの大事な仕事であることを伝える。

## 4. 入力チェック（バリデーション）

- コメント欄が空白のままでも投稿できてしまう状態を確認し、「実際のシステムでは入力漏れのチェックが必須」であることを体験する。
- VSCodeにはツールボックスがないため、[public/comments.php](public/comments.php)に用意された空欄チェックのコードを一緒に読み、短いPHPコード（`if`文での空文字チェックなど）を実際に書き加えることで、入力チェック機能を実装する体験にする。

## 5. アレンジ課題（任意・発展）

コメント表示部分の文言（例：「さん」を「様」に変更するなど）を自分で工夫して変えてみる、簡単な発展課題を用意し、「言われた通り」ではなく自分で考えて直す体験もできるようにする。

## 6. 準備のしやすさ

`start-mac.sh` / `start-win.bat` を実行するだけで、VSCodeとPHPさえ入っていれば動画共有サイトのWebアプリがその場で起動する。

---

## 体験の流れ（想定）

1. プログラミングとは何か、フロントエンドとバックエンドの違いを簡単に説明する（家づくり・料理・ラーメンの例え）
2. `start-mac.sh` などでシステムを起動し、動画共有サイトの画面を確認する
3. ログイン画面→新規登録画面へ進み、投稿ボタンの表示が分かりにくいことに気づく
4. [public/upload.php](public/upload.php) のボタン文言をテキストエディタで直接修正 → ブラウザで再読み込みして確認
5. 住所検索の仕組み（郵便番号→住所データ）について、関数の考え方として説明する
6. 会員登録を行い、ログイン状態にする（ブラウザを閉じる前にログアウトする旨を案内）
7. コメント投稿を試すが、投稿できないことを確認する
8. [public/comments.php](public/comments.php) の該当行から `//` を外し、コメント投稿を有効化 → 再度投稿して確認
9. （任意）コメント表示の文言をアレンジしてみる
10. コメントに必要な情報（コメントID・動画・投稿日時・本文）からデータベースの考え方を説明する
11. 入力漏れチェックの機能がないことを確認し、短いPHPコードを一緒に書き加えて空欄投稿がチェックされることを確認する
